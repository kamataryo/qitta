import * as React from 'react'

export default () => {

  return (
    <div className={ 'reactions' }>
      <h3>{ 'リアクション' }</h3>
      <ul className={ 'reactions' }>
        <li>{ 'good!' }</li>
        <li>{ 'Thanks!' }</li>
        <li>{ 'cool!' }</li>
      </ul>
    </div>
  )
}

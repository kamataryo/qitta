import * as React from 'react'

// interface OwnProps {
//   match: { params: { user: string } },
// }

const ProfileView = () => {
  return (
    <div id={ 'profile-view' }>
      <h1>{ 'Profile View' }</h1>
    </div>
  )
}

export default ProfileView
